version: '3.8'
services:
  api-service:
    build: ./api
    ports:
      - "5000:5000"
    volumes:
      - ./api:/app

  streamlit-app:
    build: ./streamlit
    ports:
      - "8501:8501"
    environment:
      - API_URL=http://api-service:5000
    depends_on:
      - api-service